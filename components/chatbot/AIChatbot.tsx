// This component has been deprecated in favor of the Voiceflow widget.
// Keeping the file to avoid import errors if referenced elsewhere.
// Exporting a no-op component.

'use client';

export default function AIChatbot() {
  return null;
}
